apiVersion: v1
kind: Service
metadata:
  name: {{ include "comments-api.fullname" . }}
  labels:
    {{- include "comments-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "comments-api.selectorLabels" . | nindent 4 }}
